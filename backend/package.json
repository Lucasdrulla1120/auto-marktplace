{
  "name": "auto-marketplace-backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "seed": "node prisma/seed.js",
    "setup": "npm run prisma:generate && npm run prisma:push && npm run seed",
    "deploy:prepare": "npm run prisma:generate && npm run prisma:push && npm run seed",
    "build": "node -e \"console.log('backend build ok')\"",
    "render:prepare": "prisma generate && prisma db push && node prisma/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}